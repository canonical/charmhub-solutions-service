version: '3.8'

services:
  solutions-service:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=sqlite:///solutions.db
    volumes:
      - .:/app

volumes:
  postgres_data:
